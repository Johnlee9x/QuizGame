<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.QuizGame" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.QuizGame" parent="Base.Theme.QuizGame" />

    <style name="Theme.Home" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->

        <item name="colorPrimary">@color/orange</item>

    </style>
    <style name="SignupTheme" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="android:navigationBarColor">@color/orange</item>

    </style>

    <style name="SignInTheme" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <item name="colorPrimaryDark">@color/orange</item>
        <item name="colorPrimary">@color/orange</item>
        <item name="android:toolbarStyle">@color/orange</item>
        <item name="android:navigationBarColor">@color/orange</item>
    </style>

    <style name="HomePageTheme" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="android:navigationBarColor">@color/orange</item>

    </style>
    <style name="QuizTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="android:navigationBarColor">@color/orange</item>

    </style>

</resources>